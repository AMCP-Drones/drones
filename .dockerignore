# Ignore git and IDE
.git
.gitignore
.cursor
*.swp
*~

# Built binary (rebuilt in image)
delivery-drone

# Docs and specs (not needed for build)
*.md
!vendor/**/*.md

# Tests run locally, not in image build
tests
